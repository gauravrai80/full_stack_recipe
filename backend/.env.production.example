# Example environment variables for backend deployment

# Node environment
NODE_ENV=production

# Server port
PORT=5000

# MongoDB connection string (replace with your actual MongoDB Atlas URI)
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/recipes?retryWrites=true&w=majority

# CORS origin (replace with your actual Netlify URL)
CORS_ORIGIN=https://your-app.netlify.app
